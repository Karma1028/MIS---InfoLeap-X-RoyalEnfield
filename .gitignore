# Secrets — never commit
.streamlit/secrets.toml
data/.settings_key
data/api_keys.enc

# Raw research data — proprietary. This repo is PRIVATE (confirmed before
# this exception was added), so the specific required runtime files below
# are allowed through; everything else in data/ (monthly_drops/, generated
# caches, etc.) stays ignored.
data/*
!data/Enroute_Fourth Wave_Masterfile_Base_4010_AUG-MAY.xlsx
!data/MIS_datamap.xlsx
!data/Enroute_AP_V2_netting.xlsx
!data/dq2_netting_codebook.json
!data/users.xlsx

# Caches / build artifacts
__pycache__/
*.pyc
.pytest_cache/
graphify-out/

# Local logs / scratch / verification screenshots
*.log
*.txt
!requirements.txt
*.png
*.html
page_dump.html

# Old/inactive prototype code (kept locally, not part of the deployed app)
docs/
tests/
scrape_fresh.py
build_methodology_doc.py
